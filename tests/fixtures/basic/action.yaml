name: GitHub Action Fixture (Yaml)
description: A GitHub Action Fixture (Yaml)

inputs:
  type:
    description: The type of greeting
    required: true

  who-to-greet:
    description: Who to greet
    required: true

outputs:
  message:
    description: The greeting message

runs:
  using: node20
  main: dist/index.js
